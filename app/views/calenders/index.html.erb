<%= link_to "イベント追加" ,new_calender_path %>

<%= month_calendar events: @calenders do |date, calenders| %>
  <%= date.day %>

<div class = "calendar-coler">
    <% calenders.each do |calender| %>
      <div>
        <%= calender.title %>
      </div>
    <% end %>
  <% end %>
</div>

